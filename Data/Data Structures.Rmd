DATA STRUCTURES
========================================================

There are a couple common data structures

A Vector of 3 numbers can be made by doing such:
```{r}
a <- c(0,1, 3)
a
```

there were `r nrow(Theoph)` rows in the data